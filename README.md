> [!TIP]
> Compilation: `mkdir build && cd build && cmake .. && cmake --build .`
> <br />
> Usage: `wavcodec <wav-file>`
> Controls: Vim bindings (`h` to seek back, `k` to play/pause, `l` to seek forward, `r` to restart, `q` to quit)
